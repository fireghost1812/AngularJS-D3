doctype html
html
    head
        meta(charset="utf-8")
        link(href="/build/combined.css", rel="stylesheet", type="text/css")

        title= pageTitle
    body(data-ng-app="AngularJS-D3App")
        #container
            .kit-editor(kit-editor)

    script(src="/build/combined.js")